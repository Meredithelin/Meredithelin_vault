- **昨天**:: [[2026-01-24]]
- **去年今日**:: [[2025-01-25]]
- **明天**:: [[2026-01-26]]
- **明年今日**:: [[2027-01-25]]
### 本周计划

  

  

### 任务清单
<%*
// 1. 智能计算昨天日期 (基于文件名，防报错)
const yesterday = tp.date.now("YYYY-MM-DD", -1, tp.file.title, "YYYY-MM-DD");

// ========================================================
// 2. 【关键设置】请确认你的日记存在哪个文件夹？
// 如果在 "Daily" 文件夹，就保留 "Daily"
// 如果在 "日记" 文件夹，请把下面的 "Daily" 改成 "日记"
const folderName = "日记"; 
// ========================================================

// 3. 拼接完整路径
const path = folderName ? `${folderName}/${yesterday}.md` : `${yesterday}.md`;
const file = app.vault.getAbstractFileByPath(path);

// 4. 读取文件并提取任务
if (file) {
    const content = await app.vault.read(file);
    // 正则：查找 "- [ ]" 开头的行
    const incompleteTasksRegex = /^\s*- \[ \].+$/gm;
    const tasks = content.match(incompleteTasksRegex);

    if (tasks && tasks.length > 0) {
        // 使用 tR += 输出内容
        tR += "### ⚡️ 昨日未竟之事\n" + tasks.join('\n');
    } else {
        tR += "✅ 昨日事昨日毕！";
    }
} else {
    tR += "🌴 未找到昨日日志 (" + yesterday + ")，轻装上阵！";
}
%>
- [ ] 
  

### 日记
&ensp;&ensp;&ensp;&ensp;
  

#### TIME

  

  

### 今日总结/感悟

  

#### 今日总结

  

  

#### 今日感悟
